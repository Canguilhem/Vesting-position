/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findClaimReceiptPda } from "../pdas";
import { VESTING_POSITIONS_PROGRAM_ADDRESS } from "../programs";

export const CLOSE_RECEIPT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  126, 254, 244, 203, 124, 164, 134, 89,
]);

export function getCloseReceiptDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_RECEIPT_DISCRIMINATOR
  );
}

export type CloseReceiptInstruction<
  TProgram extends string = typeof VESTING_POSITIONS_PROGRAM_ADDRESS,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountCampaign extends string | AccountMeta<string> = string,
  TAccountClaimReceipt extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUser extends string
        ? WritableSignerAccount<TAccountUser> & AccountSignerMeta<TAccountUser>
        : TAccountUser,
      TAccountCampaign extends string
        ? ReadonlyAccount<TAccountCampaign>
        : TAccountCampaign,
      TAccountClaimReceipt extends string
        ? WritableAccount<TAccountClaimReceipt>
        : TAccountClaimReceipt,
      ...TRemainingAccounts,
    ]
  >;

export type CloseReceiptInstructionData = { discriminator: ReadonlyUint8Array };

export type CloseReceiptInstructionDataArgs = {};

export function getCloseReceiptInstructionDataEncoder(): FixedSizeEncoder<CloseReceiptInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLOSE_RECEIPT_DISCRIMINATOR })
  );
}

export function getCloseReceiptInstructionDataDecoder(): FixedSizeDecoder<CloseReceiptInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCloseReceiptInstructionDataCodec(): FixedSizeCodec<
  CloseReceiptInstructionDataArgs,
  CloseReceiptInstructionData
> {
  return combineCodec(
    getCloseReceiptInstructionDataEncoder(),
    getCloseReceiptInstructionDataDecoder()
  );
}

export type CloseReceiptAsyncInput<
  TAccountUser extends string = string,
  TAccountCampaign extends string = string,
  TAccountClaimReceipt extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  campaign: Address<TAccountCampaign>;
  claimReceipt?: Address<TAccountClaimReceipt>;
};

export async function getCloseReceiptInstructionAsync<
  TAccountUser extends string,
  TAccountCampaign extends string,
  TAccountClaimReceipt extends string,
  TProgramAddress extends Address = typeof VESTING_POSITIONS_PROGRAM_ADDRESS,
>(
  input: CloseReceiptAsyncInput<
    TAccountUser,
    TAccountCampaign,
    TAccountClaimReceipt
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  CloseReceiptInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountCampaign,
    TAccountClaimReceipt
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? VESTING_POSITIONS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    campaign: { value: input.campaign ?? null, isWritable: false },
    claimReceipt: { value: input.claimReceipt ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.claimReceipt.value) {
    accounts.claimReceipt.value = await findClaimReceiptPda({
      campaign: getAddressFromResolvedInstructionAccount(
        "campaign",
        accounts.campaign.value
      ),
      user: getAddressFromResolvedInstructionAccount(
        "user",
        accounts.user.value
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("user", accounts.user),
      getAccountMeta("campaign", accounts.campaign),
      getAccountMeta("claimReceipt", accounts.claimReceipt),
    ],
    data: getCloseReceiptInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseReceiptInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountCampaign,
    TAccountClaimReceipt
  >);
}

export type CloseReceiptInput<
  TAccountUser extends string = string,
  TAccountCampaign extends string = string,
  TAccountClaimReceipt extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  campaign: Address<TAccountCampaign>;
  claimReceipt: Address<TAccountClaimReceipt>;
};

export function getCloseReceiptInstruction<
  TAccountUser extends string,
  TAccountCampaign extends string,
  TAccountClaimReceipt extends string,
  TProgramAddress extends Address = typeof VESTING_POSITIONS_PROGRAM_ADDRESS,
>(
  input: CloseReceiptInput<
    TAccountUser,
    TAccountCampaign,
    TAccountClaimReceipt
  >,
  config?: { programAddress?: TProgramAddress }
): CloseReceiptInstruction<
  TProgramAddress,
  TAccountUser,
  TAccountCampaign,
  TAccountClaimReceipt
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? VESTING_POSITIONS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    campaign: { value: input.campaign ?? null, isWritable: false },
    claimReceipt: { value: input.claimReceipt ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("user", accounts.user),
      getAccountMeta("campaign", accounts.campaign),
      getAccountMeta("claimReceipt", accounts.claimReceipt),
    ],
    data: getCloseReceiptInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseReceiptInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountCampaign,
    TAccountClaimReceipt
  >);
}

export type ParsedCloseReceiptInstruction<
  TProgram extends string = typeof VESTING_POSITIONS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    user: TAccountMetas[0];
    campaign: TAccountMetas[1];
    claimReceipt: TAccountMetas[2];
  };
  data: CloseReceiptInstructionData;
};

export function parseCloseReceiptInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCloseReceiptInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      }
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      user: getNextAccount(),
      campaign: getNextAccount(),
      claimReceipt: getNextAccount(),
    },
    data: getCloseReceiptInstructionDataDecoder().decode(instruction.data),
  };
}
